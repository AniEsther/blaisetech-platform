# Copy this file and rename the copy to ".env" then fill in your real value.

# Paystack PUBLIC key (safe to expose in frontend code — this is the "public"
# key by design). Get it from https://dashboard.paystack.com/#/settings/developer
VITE_PAYSTACK_PUBLIC_KEY=pk_test_your_public_key_here
